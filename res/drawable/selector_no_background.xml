<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Pressed -->
    <item android:drawable="@color/list_pressed_color" android:state_pressed="true"/>
    
    <!-- Focused states -->
    <item android:drawable="@drawable/shape_stroke_focused_no_background" android:state_selected="true"/>
    <item android:drawable="@drawable/shape_stroke_focused_no_background" android:state_focused="true"/>

    <!-- Non focused states -->
    <item android:drawable="@android:color/transparent"/>

</selector>